#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    while(n--){
    int cnt=0;
    string a,b;
    cin>>a>>b;
    string p=a+b;
    sort(p.begin(), p.end());
    for(int i=0;i<3;i++){
        if(p[i]!=p[i+1])cnt++;
    }
    cout<<cnt<<endl;
}}
